# claude

이 저장소의 성격과 작업 규칙은 `AGENTS.md` 를 따른다. 아래는 그 위에 얹는
대화 설정이다.

## 대화 언어

- **한국어로 답변한다.** 사용자가 다른 언어로 물어도, 명시적으로 다른 언어를
  요청하지 않는 한 답변은 한국어로 쓴다.
- 코드, 식별자, 명령어, 로그, 커밋 메시지, 파일 내용은 번역하지 않는다.
  설명만 한국어로 쓰고 기술 용어는 원문을 그대로 둔다.
